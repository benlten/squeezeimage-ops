
route:
  receiver: 'log'
  group_by: ['alertname', 'site']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 2h

receivers:
- name: 'log'
  webhook_configs:
  - url: 'http://grafana:3000/api/prom/alert-receiver' # placeholder, replace with Slack/Email webhook or Grafana contact point
